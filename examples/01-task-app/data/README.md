# Data

This folder stores structured data used by this example or application.

## Purpose

Use this folder for information that the application reads, writes, demonstrates, or uses during testing.

Typical contents may include:

- JSON files;
- sample records;
- mock form submissions;
- task definitions;
- event data;
- configuration examples;
- lookup tables;
- exported test results;
- small datasets used by the interface.

## Example structure

```text
data/
├── task-definitions.json
├── sample-events.json
├── test-records.json
└── README.md
